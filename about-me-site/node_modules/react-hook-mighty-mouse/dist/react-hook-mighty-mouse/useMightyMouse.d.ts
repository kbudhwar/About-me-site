import { Mouse } from './types';
declare const useMightyMouse: (touchEnabled?: boolean, selectedElementId?: string | null, selectedElementOffset?: {
    x: number;
    y: number;
}) => Mouse;
export default useMightyMouse;
